builds/
CMakeUserPresets.json
.vscode/settings.json
.vscode/launch.json
.sonarlint/connectedMode.json

# Emscripten build output, placed directly in web/ alongside its tracked
# HTML/CSS/JS/favicon files (see src/CMakeLists.txt's RUNTIME_OUTPUT_DIRECTORY) -
# regenerated by the build, not meant to be committed.
web/gbemu.js
web/gbemu.wasm
web/gbemu.wasm.map
